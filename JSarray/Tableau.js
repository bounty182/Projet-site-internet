let tab = [5,7,6,0,-2,1,3];
console.log(tab[2]);

tab.push(8);  // ajout d'une case a la fin du tableau

tab.pop();    // on retire la derniere case du tableau et retourne sa valeur ici => 8

console.log(tab.pop()); // on supprime la derniere case + affichage dans la console 

tab.length;

console.log(tab.length);

tab[3] = 5;             // permet de changer la valeur de la 4eme case du tableau

console.log(tab[3]);    // affiche la nouvelle valeur du tableau dans la console

